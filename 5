/******************************************************************************
Elabore um algoritmo para obter 10 valores do usuário e exibir a quantidade de valores negativos
(menores que zero) que foram informados.
Casos de teste:
a) Para os valores 1, -1, 10, 21, -14, 30, -1, 8, -9 e -20, a quantidade de valores negativos é 5;
*******************************************************************************/
#include <stdio.h>

int main() {
    int valor, negativos = 0, i = 0;

 
    while (i < 10) {
        printf("Digite o valor %d: ", i + 1);
        scanf("%d", &valor);  

        if (valor < 0) {  
        negativos++;  
        }

        i++;  
    }

    printf("A quantidade de valores negativos informados é: %d\n", negativos);

    return 0;
}
